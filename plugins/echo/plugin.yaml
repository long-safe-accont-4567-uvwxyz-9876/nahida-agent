id: echo
name: "Echo Plugin"
version: "0.1.0"
entrypoint: "echo_plugin:EchoPlugin"
description: "示例插件，回显用户消息"

permissions:
  memory:
    read: true

capabilities:
  tools:
    - name: echo
      description: "回显输入的文本"
